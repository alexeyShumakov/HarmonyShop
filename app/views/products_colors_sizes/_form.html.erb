<%= form_for(@products_colors_size) do |f| %>
  <% if @products_colors_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@products_colors_size.errors.count, "error") %> prohibited this products_colors_size from being saved:</h2>

      <ul>
      <% @products_colors_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :warehouse %><br>
    <%= f.number_field :warehouse %>
  </div>
  <div class="field">
    <%= f.label :products_color_id %><br>
    <%= f.text_field :products_color_id %>
  </div>
  <div class="field">
    <%= f.label :size_id %><br>
    <%= f.text_field :size_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
